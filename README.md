# TP Docker – Tetris + scores PostgreSQL

Support de TP : voir [FICHE_TP.md](./FICHE_TP.md).

Démarrage rapide :

```bash
cp .env.example .env   # puis éditer le mot de passe
docker compose up --build
```

Jeu accessible sur `http://localhost:5000`.
